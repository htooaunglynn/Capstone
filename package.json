{
  "name": "skillsprint-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "cf:check": "wrangler check",
    "cf:deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.2.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
